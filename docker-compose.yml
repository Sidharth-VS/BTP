services:

  # ---------------------------------------------------------------------------
  # Coordinator — FastAPI (REST :8000) + Flower gRPC server (:9091)
  # ---------------------------------------------------------------------------
  coordinator:
    build:
      context: .
      dockerfile: coordinator/Dockerfile
    container_name: fedrag-coordinator
    ports:
      - "${COORDINATOR_PORT:-8000}:8000"
      - "${FLOWER_PORT:-9091}:9091"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./workspace/coordinator:/workspace/coordinator
      - ./coordinator:/app/coordinator
      - ./coordinator/config.yaml:/app/coordinator/config.yaml:ro
    environment:
      - COORDINATOR_CONFIG=/app/coordinator/config.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-llama3.2:3b}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      fedrag-net:
        aliases:
          - coordinator
          - fedrag-coordinator

  # ---------------------------------------------------------------------------
  # Node-1 (finance domain)
  # ---------------------------------------------------------------------------
  node-1:
    build:
      context: .
      dockerfile: nodes/Dockerfile
    container_name: fedrag-node-1
    environment:
      - NODE_ID=node-1
      - NODES_CONFIG=/app/nodes/nodes.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
    volumes:
      - ./workspace/chroma/node-1:/workspace/chroma/node-1
      - ./workspace/data/node-1:/workspace/data/node-1:ro
      - ./nodes/nodes.yaml:/app/nodes/nodes.yaml:ro
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

  # ---------------------------------------------------------------------------
  # Node-2 (medical domain)
  # ---------------------------------------------------------------------------
  node-2:
    build:
      context: .
      dockerfile: nodes/Dockerfile
    container_name: fedrag-node-2
    environment:
      - NODE_ID=node-2
      - NODES_CONFIG=/app/nodes/nodes.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
    volumes:
      - ./workspace/chroma/node-2:/workspace/chroma/node-2
      - ./workspace/data/node-2:/workspace/data/node-2:ro
      - ./nodes/nodes.yaml:/app/nodes/nodes.yaml:ro
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

  # ---------------------------------------------------------------------------
  # Node-3 (legal domain)
  # ---------------------------------------------------------------------------
  node-3:
    build:
      context: .
      dockerfile: nodes/Dockerfile
    container_name: fedrag-node-3
    environment:
      - NODE_ID=node-3
      - NODES_CONFIG=/app/nodes/nodes.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
    volumes:
      - ./workspace/chroma/node-3:/workspace/chroma/node-3
      - ./workspace/data/node-3:/workspace/data/node-3:ro
      - ./nodes/nodes.yaml:/app/nodes/nodes.yaml:ro
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

  # ---------------------------------------------------------------------------
  # Node-4 (computer science / tech domain)
  # ---------------------------------------------------------------------------
  node-4:
    build:
      context: .
      dockerfile: nodes/Dockerfile
    container_name: fedrag-node-4
    environment:
      - NODE_ID=node-4
      - NODES_CONFIG=/app/nodes/nodes.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
    volumes:
      - ./workspace/chroma/node-4:/workspace/chroma/node-4
      - ./workspace/data/node-4:/workspace/data/node-4:ro
      - ./nodes/nodes.yaml:/app/nodes/nodes.yaml:ro
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

  # ---------------------------------------------------------------------------
  # Node-5 (hardware security domain)
  # ---------------------------------------------------------------------------
  node-5:
    build:
      context: .
      dockerfile: nodes/Dockerfile
    container_name: fedrag-node-5
    environment:
      - NODE_ID=node-5
      - NODES_CONFIG=/app/nodes/nodes.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
    volumes:
      - ./workspace/chroma/node-5:/workspace/chroma/node-5
      - ./workspace/data/node-5:/workspace/data/node-5:ro
      - ./nodes/nodes.yaml:/app/nodes/nodes.yaml:ro
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

  # ---------------------------------------------------------------------------
  # Node-6 (computer vision domain)
  # ---------------------------------------------------------------------------
  node-6:
    build:
      context: .
      dockerfile: nodes/Dockerfile
    container_name: fedrag-node-6
    environment:
      - NODE_ID=node-6
      - NODES_CONFIG=/app/nodes/nodes.yaml
      - API_KEY=${API_KEY:-fedrag-dev-key}
    volumes:
      - ./workspace/chroma/node-6:/workspace/chroma/node-6
      - ./workspace/data/node-6:/workspace/data/node-6:ro
      - ./nodes/nodes.yaml:/app/nodes/nodes.yaml:ro
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

  # ---------------------------------------------------------------------------
  # Frontend UI (Streamlit :8501)
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: fedrag-frontend
    ports:
      - "8501:8501"
    volumes:
      - ./frontend:/app
    environment:
      - COORDINATOR_API_URL=${COORDINATOR_API_URL:-http://fedrag-coordinator:8000/api/v1/query}
      - API_KEY=${API_KEY:-fedrag-dev-key}
    depends_on:
      coordinator:
        condition: service_healthy
    networks:
      - fedrag-net

networks:
  fedrag-net:
    driver: bridge